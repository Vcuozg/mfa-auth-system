# Database
DATABASE_URL="file:./dev.db"

# JWT Secret - tạo bằng: openssl rand -hex 64
JWT_SECRET="your-very-long-random-jwt-secret-here"

# MFA Encryption Key (AES-256 = 32 bytes = 64 hex chars)
# Tạo bằng: openssl rand -hex 32
MFA_ENCRYPTION_KEY="your-64-hex-character-encryption-key-here"
